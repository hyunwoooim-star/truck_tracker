# 🔍 버튼 반응 없음 문제 디버깅 가이드

## 현재 상황
- 회원가입 버튼을 눌러도 반응이 없음
- 사장님 로그인 버튼을 눌러도 반응이 없음

## 🛠️ 수정 사항
모든 버튼에 강화된 디버그 로깅을 추가했습니다:

### 추가된 로그
1. **화면 초기화 로그** - LoginScreen이 로드될 때
2. **이메일 로그인/회원가입 버튼** - 버튼을 누를 때마다 상세 정보 출력
3. **사장님 로그인 버튼** - 버튼 누름 및 네비게이션 시작 로그
4. **로그인/회원가입 토글 버튼** - 모드 전환 시 로그
5. **둘러보기 버튼** - 게스트 모드 진입 로그

## 📱 테스트 방법

### 1단계: 앱 실행
```bash
cd C:\Users\plmqa\OneDrive\Desktop\truck_tracker

# 웹 브라우저에서 실행 (권장)
flutter run -d chrome

# 또는 Edge에서 실행
flutter run -d edge
```

**참고:** 현재 Android SDK가 설치되어 있지 않아 안드로이드 폰으로 직접 테스트할 수 없습니다.

### 2단계: 콘솔 로그 확인
앱이 실행되면 콘솔에 다음 로그가 나타나야 합니다:

```
🎬 LoginScreen initialized
   _isLogin: true
   _isLoading: false
```

### 3단계: 버튼 테스트

#### A. 회원가입 버튼 테스트
1. "계정이 없으신가요? 회원가입" 버튼 클릭
2. 콘솔에 다음 로그가 나타나는지 확인:
   ```
   🔄 Toggling login/signup mode
      Current _isLogin: true
      New _isLogin: false
   ✅ Mode switched to 회원가입
   ```

3. 이메일과 비밀번호 입력 (예: test@test.com, password123)
4. 두 개의 동의 체크박스 모두 체크
5. "회원가입" 버튼 클릭
6. 콘솔에 다음 로그가 나타나는지 확인:
   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   🔐 LoginScreen: _handleEmailAuth called
      isLogin: false
      email: test@test.com
      password length: 11
      _isLoading: false
      _agreedToTerms: true
      _agreedToPrivacy: true
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   📧 Attempting email sign up...
   ```

#### B. 사장님 로그인 버튼 테스트
1. "사장님으로 시작하기 (테스트)" 버튼 클릭
2. 콘솔에 다음 로그가 나타나는지 확인:
   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   🚚 Owner test login button pressed
      _isLoading: false
      Navigating to OwnerDashboardScreen...
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ✅ Navigation initiated
   ```

#### C. 둘러보기 버튼 테스트
1. "둘러보기" 버튼 클릭
2. 콘솔에 다음 로그가 나타나는지 확인:
   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   👤 Guest mode button pressed
      Navigating to TruckListScreen...
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ✅ Navigation to TruckListScreen initiated
   ```

## 🐛 문제 진단

### Case 1: 버튼을 눌렀는데 아무 로그도 안 나옴
**원인:** 버튼의 onPressed가 null이거나 비활성화되어 있음
**해결:**
- `_isLoading` 상태 확인 (false여야 함)
- Hot Reload 실행: 콘솔에서 `r` 키 입력
- Hot Restart 실행: 콘솔에서 `R` 키 입력

### Case 2: 로그는 나오는데 화면이 전환되지 않음
**원인:**
- Firebase 초기화 실패
- 네비게이션 에러
- 에러가 발생했지만 catch되지 않음

**확인 사항:**
```bash
# Firebase 설정 파일 확인
cat lib/firebase_options.dart

# 필요한 패키지 확인
flutter pub get
```

### Case 3: Firebase 에러 발생
**해결:**
```bash
# Firebase 재설정
flutterfire configure

# 패키지 재설치
flutter clean
flutter pub get
flutter run -d chrome
```

## 📋 체크리스트

- [ ] `flutter run -d chrome` 실행 성공
- [ ] LoginScreen 초기화 로그 확인
- [ ] 회원가입 토글 버튼 동작 확인
- [ ] 회원가입 버튼 클릭 시 로그 확인
- [ ] 사장님 로그인 버튼 클릭 시 로그 확인
- [ ] 둘러보기 버튼 클릭 시 로그 확인
- [ ] 실제 화면 전환 동작 확인

## 💡 추가 팁

### Hot Reload vs Hot Restart
- **Hot Reload (r)**: 코드 변경사항만 빠르게 반영 (상태 유지)
- **Hot Restart (R)**: 앱 전체 재시작 (상태 초기화)
- **Full Restart (q → flutter run)**: 완전 재빌드

### 콘솔 단축키
- `r`: Hot Reload
- `R`: Hot Restart
- `q`: 종료
- `h`: 도움말
- `d`: Flutter DevTools 열기

### 브라우저 개발자 도구
Chrome/Edge에서 F12를 누르면:
- Console 탭: JavaScript 에러 확인
- Network 탭: Firebase API 호출 확인
- Application 탭: LocalStorage, IndexedDB 확인

## 🔧 문제 지속 시 실행할 명령어

```bash
# 1. 완전 클린 빌드
flutter clean
flutter pub get

# 2. build_runner로 코드 재생성
flutter pub run build_runner build --delete-conflicting-outputs

# 3. 앱 재실행
flutter run -d chrome
```

## 📞 보고 양식

문제가 지속되면 다음 정보를 제공해주세요:

1. **실행 명령어:**
   ```
   flutter run -d <device>
   ```

2. **콘솔 로그:**
   ```
   (버튼을 눌렀을 때 나오는 전체 로그 복사)
   ```

3. **화면 동작:**
   - 버튼을 누르면 어떻게 되는지 설명
   - 스크린샷 첨부 (선택사항)

4. **에러 메시지:**
   ```
   (에러가 있다면 전체 스택 트레이스 복사)
   ```
